{
  "private": true,
  "name": "@ansi-escape-code/type",
  "version": "3.0.0",
  "description": "AnsiString type for ansi-escape-code",
  "repository": {
    "type": "git",
    "url": "https://github.com/basic-examples/ansi-escape-code.git",
    "directory": "packages/type"
  },
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist && tsc && cpy npm.md dist --rename=README.md && package-json-minifier",
    "publish": "npm run build && cd dist && npm publish",
    "test": "cd test && npm start"
  },
  "devDependencies": {
    "cpy-cli": "^5.0.0",
    "package.json-minifier": "^0.0.4",
    "rimraf": "^6.0.1",
    "typedoc": "^0.28.9",
    "typescript": "^5.8.3"
  }
}
